name: Daily Commit

on:
  schedule:
    - cron: '0 0 * * *'  # Runs every day at midnight UTC

jobs:
  commit:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set Git Config
      run: |
        git config user.name "KhanAnupamShafi"
        git config user.email "deep71.bd@gmail.com"

    - name: Make Changes
      run: echo "$(date)" > daily-commit.txt

    - name: Commit Changes
      run: |
        git add .
        git commit -m "$(date '+%Y-%m-%d') Draw io Anupam"

    - name: Push Changes
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.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 }}
